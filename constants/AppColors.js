/**
 * Single app palette (no light/dark variants).
 * Matches the deep red + dusty rose bottom navigation design.
 */
const AppColors = {
  /** Main screen / brand background */
  background: '#8B0000',
  /** Cards, tab bar, floating button */
  surface: '#FFFFFF',
  /** Active tab icon */
  activeIcon: '#8B0000',
  /** Inactive tab icons */
  inactiveIcon: '#A55D5D',
  /** Tab labels (active + inactive) */
  label: '#A55D5D',
  /** Primary body text on light surfaces */
  textOnLight: '#1a1a1a',
  /** Secondary / muted text on light surfaces */
  textMuted: 'rgba(0,0,0,0.65)',
  /** Hairline separators */
  separator: 'rgba(0,0,0,0.08)',
  /** Links / accents on light */
  link: '#8B0000',
};

export default AppColors;
